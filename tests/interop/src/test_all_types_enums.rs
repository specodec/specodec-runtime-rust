// Generated by generate_emit_runner.mjs. DO NOT EDIT.
use std::fs;
use std::path::Path;
use specodec::{MsgPackReader, MsgPackWriter, JsonReader, JsonWriter, GronReader, GronWriter, SpecReader, SpecWriter};
use crate::generated::all_types_enums_types::*;


fn test_model_enum_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = enum_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            enum_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL EnumHolder mp: write error"); failed += 1; }
        } else { println!("FAIL EnumHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL EnumHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = enum_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                enum_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL EnumHolder json: write error"); failed += 1; }
            } else { println!("FAIL EnumHolder json: decode error"); failed += 1; }
        } else { println!("FAIL EnumHolder json: reader error"); failed += 1; }
    } else { println!("FAIL EnumHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = enum_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                enum_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL EnumHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL EnumHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL EnumHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL EnumHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = enum_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            enum_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL EnumHolder gron: write error"); failed += 1; }
        } else { println!("FAIL EnumHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL EnumHolder gron: file not found"); failed += 1; }
    (passed, failed)
}

fn test_model_opt_enum_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptEnumHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = opt_enum_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            opt_enum_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("OptEnumHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL OptEnumHolder mp: write error"); failed += 1; }
        } else { println!("FAIL OptEnumHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL OptEnumHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptEnumHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = opt_enum_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                opt_enum_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("OptEnumHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL OptEnumHolder json: write error"); failed += 1; }
            } else { println!("FAIL OptEnumHolder json: decode error"); failed += 1; }
        } else { println!("FAIL OptEnumHolder json: reader error"); failed += 1; }
    } else { println!("FAIL OptEnumHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptEnumHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = opt_enum_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                opt_enum_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("OptEnumHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL OptEnumHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL OptEnumHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL OptEnumHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL OptEnumHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptEnumHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = opt_enum_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            opt_enum_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("OptEnumHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL OptEnumHolder gron: write error"); failed += 1; }
        } else { println!("FAIL OptEnumHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL OptEnumHolder gron: file not found"); failed += 1; }
    (passed, failed)
}

fn test_model_enum_array_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumArrayHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = enum_array_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            enum_array_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumArrayHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL EnumArrayHolder mp: write error"); failed += 1; }
        } else { println!("FAIL EnumArrayHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL EnumArrayHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumArrayHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = enum_array_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                enum_array_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumArrayHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL EnumArrayHolder json: write error"); failed += 1; }
            } else { println!("FAIL EnumArrayHolder json: decode error"); failed += 1; }
        } else { println!("FAIL EnumArrayHolder json: reader error"); failed += 1; }
    } else { println!("FAIL EnumArrayHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumArrayHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = enum_array_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                enum_array_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumArrayHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL EnumArrayHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL EnumArrayHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL EnumArrayHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL EnumArrayHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumArrayHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = enum_array_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            enum_array_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumArrayHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL EnumArrayHolder gron: write error"); failed += 1; }
        } else { println!("FAIL EnumArrayHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL EnumArrayHolder gron: file not found"); failed += 1; }
    (passed, failed)
}

fn test_model_enum_mixed_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumMixedHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = enum_mixed_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            enum_mixed_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumMixedHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL EnumMixedHolder mp: write error"); failed += 1; }
        } else { println!("FAIL EnumMixedHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL EnumMixedHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumMixedHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = enum_mixed_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                enum_mixed_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumMixedHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL EnumMixedHolder json: write error"); failed += 1; }
            } else { println!("FAIL EnumMixedHolder json: decode error"); failed += 1; }
        } else { println!("FAIL EnumMixedHolder json: reader error"); failed += 1; }
    } else { println!("FAIL EnumMixedHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumMixedHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = enum_mixed_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                enum_mixed_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumMixedHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL EnumMixedHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL EnumMixedHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL EnumMixedHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL EnumMixedHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("EnumMixedHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = enum_mixed_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            enum_mixed_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("EnumMixedHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL EnumMixedHolder gron: write error"); failed += 1; }
        } else { println!("FAIL EnumMixedHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL EnumMixedHolder gron: file not found"); failed += 1; }
    (passed, failed)
}


pub fn run(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    let (p, f) = test_model_enum_holder(vec_dir, out_dir); passed += p; failed += f;
    let (p, f) = test_model_opt_enum_holder(vec_dir, out_dir); passed += p; failed += f;
    let (p, f) = test_model_enum_array_holder(vec_dir, out_dir); passed += p; failed += f;
    let (p, f) = test_model_enum_mixed_holder(vec_dir, out_dir); passed += p; failed += f;

    (passed, failed)
}
