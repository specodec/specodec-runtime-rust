// Generated by generate_emit_runner.mjs. DO NOT EDIT.
use std::fs;
use std::path::Path;
use specodec::{MsgPackReader, MsgPackWriter, JsonReader, JsonWriter, GronReader, GronWriter, SpecReader, SpecWriter};
use crate::generated::all_types_unions_types::*;


fn test_model_union_field_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionFieldHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = union_field_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            union_field_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionFieldHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionFieldHolder mp: write error"); failed += 1; }
        } else { println!("FAIL UnionFieldHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL UnionFieldHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionFieldHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_field_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_field_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionFieldHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionFieldHolder json: write error"); failed += 1; }
            } else { println!("FAIL UnionFieldHolder json: decode error"); failed += 1; }
        } else { println!("FAIL UnionFieldHolder json: reader error"); failed += 1; }
    } else { println!("FAIL UnionFieldHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionFieldHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_field_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_field_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionFieldHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionFieldHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL UnionFieldHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL UnionFieldHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL UnionFieldHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionFieldHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = union_field_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            union_field_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionFieldHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionFieldHolder gron: write error"); failed += 1; }
        } else { println!("FAIL UnionFieldHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL UnionFieldHolder gron: file not found"); failed += 1; }
    (passed, failed)
}

fn test_model_opt_union_field_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptUnionFieldHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = opt_union_field_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            opt_union_field_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("OptUnionFieldHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL OptUnionFieldHolder mp: write error"); failed += 1; }
        } else { println!("FAIL OptUnionFieldHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL OptUnionFieldHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptUnionFieldHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = opt_union_field_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                opt_union_field_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("OptUnionFieldHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL OptUnionFieldHolder json: write error"); failed += 1; }
            } else { println!("FAIL OptUnionFieldHolder json: decode error"); failed += 1; }
        } else { println!("FAIL OptUnionFieldHolder json: reader error"); failed += 1; }
    } else { println!("FAIL OptUnionFieldHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptUnionFieldHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = opt_union_field_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                opt_union_field_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("OptUnionFieldHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL OptUnionFieldHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL OptUnionFieldHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL OptUnionFieldHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL OptUnionFieldHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("OptUnionFieldHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = opt_union_field_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            opt_union_field_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("OptUnionFieldHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL OptUnionFieldHolder gron: write error"); failed += 1; }
        } else { println!("FAIL OptUnionFieldHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL OptUnionFieldHolder gron: file not found"); failed += 1; }
    (passed, failed)
}

fn test_model_union_array_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionArrayHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = union_array_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            union_array_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionArrayHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionArrayHolder mp: write error"); failed += 1; }
        } else { println!("FAIL UnionArrayHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL UnionArrayHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionArrayHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_array_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_array_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionArrayHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionArrayHolder json: write error"); failed += 1; }
            } else { println!("FAIL UnionArrayHolder json: decode error"); failed += 1; }
        } else { println!("FAIL UnionArrayHolder json: reader error"); failed += 1; }
    } else { println!("FAIL UnionArrayHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionArrayHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_array_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_array_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionArrayHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionArrayHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL UnionArrayHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL UnionArrayHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL UnionArrayHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionArrayHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = union_array_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            union_array_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionArrayHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionArrayHolder gron: write error"); failed += 1; }
        } else { println!("FAIL UnionArrayHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL UnionArrayHolder gron: file not found"); failed += 1; }
    (passed, failed)
}

fn test_model_union_mixed_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionMixedHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = union_mixed_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            union_mixed_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionMixedHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionMixedHolder mp: write error"); failed += 1; }
        } else { println!("FAIL UnionMixedHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL UnionMixedHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionMixedHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_mixed_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_mixed_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionMixedHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionMixedHolder json: write error"); failed += 1; }
            } else { println!("FAIL UnionMixedHolder json: decode error"); failed += 1; }
        } else { println!("FAIL UnionMixedHolder json: reader error"); failed += 1; }
    } else { println!("FAIL UnionMixedHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionMixedHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_mixed_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_mixed_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionMixedHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionMixedHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL UnionMixedHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL UnionMixedHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL UnionMixedHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionMixedHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = union_mixed_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            union_mixed_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionMixedHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionMixedHolder gron: write error"); failed += 1; }
        } else { println!("FAIL UnionMixedHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL UnionMixedHolder gron: file not found"); failed += 1; }
    (passed, failed)
}

fn test_model_union_scalar_holder(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    // msgpack
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionScalarHolder.msgpack")) {
        let mut r = MsgPackReader::new(&b);
        if let Ok(obj) = union_scalar_holder_decode(&mut r) {
            let mut w = MsgPackWriter::new();
            union_scalar_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionScalarHolder.msgpack"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionScalarHolder mp: write error"); failed += 1; }
        } else { println!("FAIL UnionScalarHolder mp: decode error"); failed += 1; }
    } else { println!("FAIL UnionScalarHolder mp: file not found"); failed += 1; }
    // json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionScalarHolder.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_scalar_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_scalar_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionScalarHolder.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionScalarHolder json: write error"); failed += 1; }
            } else { println!("FAIL UnionScalarHolder json: decode error"); failed += 1; }
        } else { println!("FAIL UnionScalarHolder json: reader error"); failed += 1; }
    } else { println!("FAIL UnionScalarHolder json: file not found"); failed += 1; }
    // unformatted json
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionScalarHolder.unformatted.json")) {
        if let Ok(mut r) = JsonReader::new(&b) {
            if let Ok(obj) = union_scalar_holder_decode(&mut r) {
                let mut w = JsonWriter::new();
                union_scalar_holder_write(&obj, &mut w);
                if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionScalarHolder.unformatted.json"), w.to_bytes()) {
                    passed += 1;
                } else { println!("FAIL UnionScalarHolder unformatted: write error"); failed += 1; }
            } else { println!("FAIL UnionScalarHolder unformatted: decode error"); failed += 1; }
        } else { println!("FAIL UnionScalarHolder unformatted: reader error"); failed += 1; }
    } else { println!("FAIL UnionScalarHolder unformatted: file not found"); failed += 1; }
    // gron
    if let Ok(b) = fs::read(Path::new(&vec_dir).join("UnionScalarHolder.gron")) {
        let mut r = GronReader::new(&b);
        if let Ok(obj) = union_scalar_holder_decode(&mut r) {
            let mut w = GronWriter::new();
            union_scalar_holder_write(&obj, &mut w);
            if let Ok(_) = fs::write(Path::new(&out_dir).join("UnionScalarHolder.gron"), w.to_bytes()) {
                passed += 1;
            } else { println!("FAIL UnionScalarHolder gron: write error"); failed += 1; }
        } else { println!("FAIL UnionScalarHolder gron: decode error"); failed += 1; }
    } else { println!("FAIL UnionScalarHolder gron: file not found"); failed += 1; }
    (passed, failed)
}


pub fn run(vec_dir: &str, out_dir: &str) -> (u32, u32) {
    let mut passed = 0u32;
    let mut failed = 0u32;
    let (p, f) = test_model_union_field_holder(vec_dir, out_dir); passed += p; failed += f;
    let (p, f) = test_model_opt_union_field_holder(vec_dir, out_dir); passed += p; failed += f;
    let (p, f) = test_model_union_array_holder(vec_dir, out_dir); passed += p; failed += f;
    let (p, f) = test_model_union_mixed_holder(vec_dir, out_dir); passed += p; failed += f;
    let (p, f) = test_model_union_scalar_holder(vec_dir, out_dir); passed += p; failed += f;

    (passed, failed)
}
